export default {
    install: (Vue, opt) => {

        const __proto = Vue.prototype;


        /**
         * 打印
         * @param {*} options 
         */
        __proto.$print = (options) => {
            
        }
    }
}